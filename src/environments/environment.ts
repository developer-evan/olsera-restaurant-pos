export const environment = {
  production: false,
  apiBaseUrl: 'https://drainage-disbelief-malt.ngrok-free.dev/api/v1',
  /** Set true when apiBaseUrl goes through ngrok (adds skip-browser-warning header). */
  ngrok: true,
};
